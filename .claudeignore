# Mirror of .codexignore: the skill package is skills/knuaf-doc only.
# README says app/ and tests/ are development directories that do not ship;
# without this, a Claude Code marketplace install pulls the whole repo.
app/
tests/
.venv-dev/
.github/
